#include<stdio.h>
#include<stdlib.h>
#include<unistd.h>
#include<string.h>
#include<sys/type.h>

int gloval = 6;
void main(void)
{
	int autoval = 80;
	pid_t pid;

	if (fork() < 0) {
		perror("fork error");
	} else
}
