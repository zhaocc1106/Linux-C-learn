#include<stdio.h>
#include<stdlib.h>
#include<setjmp.h>

static void func1(int, int, int, int);
static void func2();

static __jmp_buf jmpbuffer;
static int globalval;

void main (void)
{
	int autioval;
	int register regival;
	volatile int volval;
	static int staval;

	globalval = 1;
	autioval = 2;
	regival = 3;
	volval = 4;
	staval = 5;

	if
}
